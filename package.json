{
  "name": "hardline",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test --isolate",
    "build": "bun run scripts/build.ts"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
